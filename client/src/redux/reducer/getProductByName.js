/*const initialState = {
  search: [],
};

export const productUnitSlice = createSlice({
  name: "search",
  initialState,
  reducers: {},
  extraReducers: {
    [getByName.pending]: () => {
      console.log("pending");
    },
    [getByName.fulfilled]: (state, action) => {
      return { ...state, serch: action.payload };
    },
  },
});

export default productUnitSlice.reducer;

export const searchResult = (state) => state.search.search;*/
